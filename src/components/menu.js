import React from "react"
import styled from "styled-components"

const MenuContent = styled.div`
    background: blue;
`

const Menu = () => (
    <MenuContent>
        coucou
    </MenuContent>
)
export default Menu